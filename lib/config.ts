// Configuration for the scheduler
export const config = {
  // API Configuration
  api: {
    // API endpoints
    endpoints: {
      jobs: '/api/jobs',
      // Add more endpoints as needed
    }
  }
};
