/**
 * Tailwind class sets for access-request / approval status pills (Track requests, My Approvals).
 */
export function getAccessRequestStatusBadgeClasses(_status: string): string {
  return "bg-blue-100 text-blue-800";
}
